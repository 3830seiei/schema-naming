#- metadata -----------------------------------------------
metadata:
  description: テーブル定義書
  author: H.Miyazawa
  history:
  - version: 1.0.0
    date: '2025-06-27'
    author: H.Miyazawa
    comment: OptiServe用のデータベース設計

#- tableinfo ----------------------------------------------
table_name: medical_device_usage_flags
description: 医療機器の分析使用フラグ管理テーブル
#- columns info -------------------------------------------
columns:
- name: medical_facility_id
  description: 医療機関ID
  data_type: integer
  primary_key: true
  nullable: false
  comment: null
  references:
    table: mst_medical_facility
    column: medical_facility_id
    enforced: false  # DB制約無し
    on_delete: restrict  # restrict, cascade, set null, no action
    on_update: restrict
    comment: 'mst_medical_facilityテーブルのmedical_facility_idを参照。医療機関ID。'
- name: medical_device_id
  description: 医療機器ID
  data_type: integer
  primary_key: true
  nullable: false
  comment: null
  references:
    table: medical_device_list
    column: medical_device_id
    enforced: false  # DB制約無し
    on_delete: restrict  # restrict, cascade, set null, no action
    on_update: restrict
    comment: 'medical_device_listテーブルのmedical_device_idを参照。医療機器ID。'
- name: is_active
  description: 使用中フラグ
  data_type: boolean
  primary_key: false
  nullable: false
  comment: '医療機器を集計対象として有効化を示すフラグ。Trueの場合は使用中、Falseの場合は使用中ではない。'
- name: regdate
  description: データ作成日
  data_type: timestamp
  primary_key: false
  nullable: false
  comment: null
- name: lastupdate
  description: 最終更新日
  data_type: timestamp
  primary_key: false
  nullable: false
  comment: null
